#!/usr/bin/env bash
set -euo pipefail
source "$(dirname "$0")/sp_common.sh"

LOG=$1

log "Applying semantic density stress"

for i in {1..5}; do
  cat "$LOG"
done | wc -l

log "High redundancy does not imply error"

