#!/usr/bin/env bash
set -euo pipefail
MODE="${1:-normal}"
LOGFILE="${2:-/tmp/spherepop.log}"

i=0
while true; do
	i=$((i+1))
	printf "[%s] pdflatex: pass=%02d  target=essay_%s.tex  (theater)\n" "$(date +%H:%M:%S)" "$i" "$MODE"
	sleep 1
done

