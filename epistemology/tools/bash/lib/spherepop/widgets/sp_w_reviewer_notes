#!/usr/bin/env bash
set -euo pipefail
MODE="${1:-reviewer}"
LOGFILE="${2:-/tmp/spherepop.log}"

cat <<'EOF'
REVIEWER NOTES (theater)
- Metrics define equivalence classes; optimization collapses semantics.
- Executability without constraint coupling = didactic interface.
- Dense exposition can be semantic impedance; truth ≠ rhetorical minimality.

Refs (suggested):
  Goodhart (1975), Polanyi (1966), Ryle (1949), Weber (1922),
  Winner (1980), Arendt (1963), Foucault (1975)
EOF
while true; do sleep 10; done

