#!/bin/sh
# sp-edge-init — initialize an explicit edge network

set -eu

CONFIG="${1:-nodes/lab.json}"

if [ ! -f "$CONFIG" ]; then
  echo "Config not found: $CONFIG" >&2
  exit 1
fi

mkdir -p state
echo "Initialized edge network from $CONFIG"

