#!/usr/bin/env bash
set -euo pipefail
source "$(dirname "$0")/sp_common.sh"

LOG=$1

log "Producing audit summary (non-procedural)"

grep -E "POP|MERGE|REFUSE" "$LOG" \
  | sed 's/POP/introduced entity/' \
  | sed 's/MERGE/declared equivalence/' \
  | sed 's/REFUSE/suspended execution/'

