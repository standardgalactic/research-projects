#!/bin/sh
# sp-edge-lab — run a named experiment across nodes

set -eu

EXPERIMENT="$1"
SCRIPT="experiments/$EXPERIMENT.sh"

if [ ! -x "$SCRIPT" ]; then
  echo "Experiment not found or not executable: $SCRIPT" >&2
  exit 1
fi

sp-edge-run "bash -s" < "$SCRIPT"

