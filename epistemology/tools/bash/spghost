#!/usr/bin/env bash
set -euo pipefail
source "$(dirname "$0")/sp_common.sh"

TARGET=${1:-.}

log "Searching for undead identifiers"

grep -R "TODO\|FIXME\|HACK\|TEMP" "$TARGET" || true

log "Ghosts indicate unresolved ontology, not bugs"

