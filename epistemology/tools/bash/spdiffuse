#!/usr/bin/env bash
set -euo pipefail
source "$(dirname "$0")/sp_common.sh"

LOG=$1

log "Injecting epsilon perturbation (comment noise)"

sed 's/$/ #noise/' "$LOG" | diff -u "$LOG" - || true

log "Observe: semantic equivalence must be argued, not assumed"

