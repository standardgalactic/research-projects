
[project]
name = "entropy_ecology_sim"
version = "0.1.0"
requires-python = ">=3.11"
description = "RSVP-based Entropy Ecology Simulator"
dependencies = [
    "jax",
    "matplotlib",
    "rich",
]

[project.optional-dependencies]
viz = ["dash", "plotly"]
